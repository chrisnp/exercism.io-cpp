#pragma once

#include <vector>
#include <numeric>

namespace sum_of_multiples {

using namespace std;

typedef vector<long> divisors;

long to(divisors factors, long up_to);

}  // namespace sum_of_multiples
